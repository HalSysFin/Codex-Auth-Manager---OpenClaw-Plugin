# Changelog

## 1.0.5 - 2026-07-16

- expose read-only banked reset availability through the lease control API
- add explicit `useRateLimitReset` support scoped to the active lease
- never redeem resets from automatic lease management

## 1.0.4 - 2026-04-01

- refreshed the release line alongside the shared Auth Manager lease/runtime updates
- keeps manager-driven materialization and telemetry aligned with the new monorepo release
